{
  "name": "neuro-chat",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Нейро-чат — AI чат на Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}